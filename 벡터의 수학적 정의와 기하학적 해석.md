# 1. 스칼라,벡터,행렬의 정의 및 표기법

| 구분 | 차원 | 표기 예시 | 예시 |
| --- | --- | --- | --- |
| 스칼라 | 0차원 | a, 3.5 | 온도 25도 |
| 벡터 | 1차원 | v = (v1, v2, v3) | 위치 좌표 (2, 3) |
| 행렬 | 2차원 | A (m x n) | 데이터 테이블 |

```python
import numpy as np

scalar = 3.5
vector = np.array([2, 3])
matrix = np.array([[1, 2], [3, 4]])
```
# 2. 벡터의 덧셈과 스칼라 곱

# 벡터의 덧셈
벡터의 덧셈은 같은 위치에 있는 성분끼리 더하는 연산
두 벡터 a = (a1, a2)와 b = (b1, b2)가 있으면 a + b = (a1+b1, a2+b2)로 계산해요.
벡터 a의 끝에 벡터 b를 이어 붙였을 때 도착하는 지점이 a+b가 돼요. 이를 평행사변형 법칙이라고 부름

# 스칼라 곱
스칼라 곱은 벡터의 모든 성분에 같은 숫자(스칼라)를 곱하는 연산
c가 스칼라이고 v = (v1, v2)일 때 cv = (cv1, c*v1)으로 계산
스칼라 곱은 벡터의 길이를 늘리거나 줄이는 역할을 함 c가 음수면 방향이 반대로 뒤집힘

a = np.array([1, 2])
b = np.array([3, 1])

vector_sum = a + b        # array([4, 3]) #벡터의 덧셈
scalar_mul = 2 * a        # array([2, 4]) #스칼라 곱

# 벡터의 크기(노름), 방향, 단위벡터

# 노름(norm)
노름은 벡터의 크기, 즉 길이를 나타내는 값
2차원 벡터 v = (v1, v2)의 노름은 피타고라스 정리를 이용해 다음과 같이 계산함
||v|| = sqrt(v1^2 + v2^2)
일반적인 n차원 벡터에서도 각 성분을 제곱해 더한 뒤 제곱근을 취하는 방식은 동일함
이를 유클리드 노름(L2 norm)이라고 부름

# 단위벡터와 정규화
단위벡터는 노름이 1인 벡터를 말함
어떤 벡터든 자기 자신의 노름으로 나누면 방향은 그대로 유지한 채 크기만 1로 만들 수 있는데,
이 과정을 정규화(normalization)라고 함
unit_v = v / ||v|| 

v = np.array([3, 4])
norm_v = np.linalg.norm(v)   # 5.0 norm
unit_v = v / norm_v          # array([0.6, 0.8]) 정규화

# 정규화와 ML 특성 벡터의 의미
머신러닝에서 정규화가 중요한 이유

머신러닝 모델에 입력되는 데이터는 여러 특성(feature)을 가진 벡터로 표현됨
그런데 특성마다 값의 범위가 크게 다르면(예:나이는 0~100, 연봉은 0~1억)
크기가 큰 특성이 계산결과를 지배해버릴 수 있음

정규화를 통해 벡터의 방향은 유지하면서 크기를 통일하면, 모델이 특정 특성에 치우치지 않고,
공정하게 학습할 수 있음.